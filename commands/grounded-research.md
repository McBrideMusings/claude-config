---
description: Run the grounded-research skill on a request — extract direct quotes, cite every claim, admit uncertainty
---

Invoke the `grounded-research` skill using the Skill tool.

The user's request: $ARGUMENTS

If $ARGUMENTS is empty, ask the user what they want researched, then invoke the skill on their answer.
